services:
  node:
    image: node:22
    ports:
      - "5173:5173"
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules 
    command: ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0"]
