import React from 'react'
import {Card, Typography, Description, Tag} from 'antd'
import { ShoppingOutlined } from '@ant-design/icons'

const {Text} = Typography;

export default function OrderInformation({order, tra}) {
  return (
    <div>
      
    </div>
  )
}
